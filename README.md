# CleanOps Agent

Vertical SaaS for commercial cleaning operations. Web frontend (Next.js) and API backend (NestJS) with Prisma and PostgreSQL. Run locally with Docker Compose.

## Local Run

docker compose up --build
