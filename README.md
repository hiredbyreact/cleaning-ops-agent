# Cleaning Ops Agent

Monorepo scaffold for a vertical SaaS startup: CleanOps AI for commercial cleaning operations. This repository provides a minimal runnable stack (web frontend + api backend) with a Postgres-backed Prisma ORM.

## Local Run

docker compose up --build
