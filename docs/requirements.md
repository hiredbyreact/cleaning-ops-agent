# System requirements

- Node.js 18+ (we run Node 20 in Docker) 
- pnpm workspace with pnpm@8
- PostgreSQL 13+
- Docker and Docker Compose

Local development runs the two services in docker-compose:
- web: Next.js frontend
- api: NestJS API
