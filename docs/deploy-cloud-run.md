# Deploy to Cloud Run

- Build separate container images for web and api
- Push to a container registry
- Create two Cloud Run services wired to the images
- Configure environment variables (DATABASE_URL, etc.)

This document provides a generic deployment guide; customize per your cloud provider.
