{
  "name": "startup-box",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -w build",
    "start": "echo \"Use docker-compose to run locally.\""
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "prisma"
  ]
}